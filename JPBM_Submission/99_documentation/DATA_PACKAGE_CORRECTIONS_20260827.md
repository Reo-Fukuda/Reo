# 生データ再分析ノート(2026-08-27)

## 実施したこと

外部レビュー項目4(構成概念妥当性)を、Dropbox の公開データファイルから実際に計算して解決。

データ所在: `/【研究】/【メタバース】/Revision/NFT_Freemium/HAD分析結果/aspredicted/`
使用ファイル: `study2_public.csv` / `study3a_public.csv` / `study3b_public.csv` / `study3c_public.csv`

分析: 最尤法CFA(因子分散を1に固定・各項目は所属因子にのみ負荷)を自前実装し、
`factor_analyzer` の `ConfirmatoryFactorAnalyzer` と突き合わせて検証済み
(標準化負荷量・因子間相関が小数第3位まで一致、χ²は本実装のほうが 24.037 対 24.058 とわずかに良好)。
負荷量の標準誤差は ML 判別関数の数値ヘッセ行列から算出。

## 原稿との照合結果(すべて一致)

| Study | 論文のN | データのN | 除外 | α(論文) | α(再計算) |
|---|---|---|---|---|---|
| 2 | 280 | 283 → 280 | attention_1≠1 が3名 | LPL .85 / LBL .94–.96 / IC .91 | .849 / .957 / .907 |
| 3a | 138 | 140 → 138 | 同 2名 | FI .87–.91 / LBL .94–.96 | .911 / .952 |
| 3b | 281 | 282 → 281 | 同 1名 | 同上 | .873 / .954 |
| 3c | 282 | 287 → 282 | 同 5名 | 同上 | .866 / .942 |

Study 2 の操作チェック(S2-4)も再現: 物理 M=4.69 SD=1.32、デジタル M=3.42 SD=1.60、
Welch t(252.00)=7.15、d=0.87(論文は t=7.16。小数第2位のみの差)。

## 要判断・要修正(著者へ)

1. **コードブックの項目リストが古い(データ側の問題。原稿は正しい)**
   - `study3a_codebook.csv`: Failure Inference を4項目(fail_inf_1,2,3,5、α=.912)と記載。
     実際の `study3a_public.csv` は3項目のみで、3項目のαは .911。
   - `study3b_codebook.csv`: 同じく5項目(fail_inf_1〜5)と記載。α=.873 と記載。
     実データは3項目で、3項目のαはちょうど .873。
   - → **αはどちらも3項目で計算されている**ため、コードブックの「項目リスト」だけが
     旧い項目プールを引きずっている。公開時に監査者を確実に混乱させるので要修正。

2. **`low_flagship` の変数名が内容と逆に読める**
   - Study 3b: `low_flagship=1` の群のほうが Flagshipness_MC が高い(M=4.94 対 2.67)。
     コードブックの値ラベル(0=non-flagship, 1=flagship)は正しく、**変数名だけ**が誤解を招く。
   - Study 3c では同じ変数がソース操作(公式サイト/ニュース記事)に流用されている。
   - → 公開版ではリネーム(例 `high_flagship` / `source_official`)を推奨。

3. **Failure Inference の改名**: 未決のまま。今回は改名せず、レビューのもう一方の選択肢
   (初出定義の明確化)を採用した。改名する場合は SI H-4 と本文の該当箇所も差し替えが必要。

4. **新規追加した3文献のDOI・巻号は要確認**: Fornell & Larcker (1981)、
   Henseler et al. (2015)、Kenny et al. (2015)。

5. 参考文献リストの並び(本文): `Hagtvedt` → `Han` → `Ha, Y.-W.` の順になっており、
   `Ha` が本来より後ろにある(既存の問題)。

## LaTeX コンパイル検証

TeX Live 2023 で本文・SI とも **エラー0・Overfull 0** を確認(図はダミー差し替え)。
